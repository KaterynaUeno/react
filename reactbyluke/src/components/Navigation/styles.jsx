import { css } from '@emotion/react/macro';

const theme = () => css`
    .test {
        background: red;
        color: green;
    }

    background: green;
    color: blue;
`;

export default theme;
